# 이 저장소에서 일하는 법

## 커밋 메시지

```
type(scope): 명사형 요약

- 개조식 불릿. 각 줄 명사형 종결
- 한 줄에 한 사실. 최대 5줄
```

`type` 은 feat · fix · docs · chore.
`scope` 는 web · api · ai · db · contracts · design · deploy · build · dev. 생략 금지.

**제목**

- 명사형으로 끝낸다. `매칭한다` → `매칭`, `내린다` → `내림`
- **고친 결과를 적는다. 증상을 적지 않는다.**
  `~던 문제`, `~하던 현상`, `~ 오류`, `~ 수정`, `~ 보강` 으로 끝내지 않는다.
  무엇이 잘못됐는지는 본문 첫 줄에 한 번만 적는다
- 50자 이내

```
나쁨   fix(web): 이름 수정 시 주기가 어긋나던 문제
좋음   fix(web): 이름을 고쳐도 사용자가 정한 주기 유지

나쁨   fix(deploy): API 이미지에 parser 패키지 누락 수정
좋음   fix(deploy): API 이미지에 parser 패키지 포함
```

**본문**

- 개조식으로 쓴다. 줄글로 풀어 쓰지 않는다
- 각 줄은 명사형으로 끝낸다. `~했다`, `~된다`, `~해야 한다` 금지
- 비유를 쓰지 않는다. `빠져나갈 길`, `두드린다` → `대체 경로`, `요청`
- 사실만 적는다. 발견 과정·검증 방법·제품 철학은 넣지 않는다
- ASCII 표와 before/after 도식을 넣지 않는다

```
나쁨   후보가 없으면 저장할 길이 아예 없었다. AI 가 못 알아들으면
       방금 한 일이 없던 일이 됐다.

좋음   - 후보 없을 때 저장 경로 부재로 입력 유실
       - 재시도 시트에 항목명 입력 필드 추가
```

**트레일러를 붙이지 않는다.** `Co-Authored-By` 도, 도구 생성 표기도 넣지 않는다.

## 커밋은 끝난 뒤에 한 번만

한 가지 변경은 커밋 하나다. 고치다 만 것을 올리고 다음 커밋에서 마저 고치지 않는다.

지저분한 이력은 대부분 **덜 끝난 것을 올려서** 생긴다. 이 저장소에 실제로 있었던 것:

```
517b5ee  refactor: 규칙 파서를 공용 패키지로 분리     ← 배포용 Dockerfile 을 안 고침
  … 커밋 4개 뒤 …
8260c31  fix(deploy): API 이미지에 parser 패키지 누락  ← 그 뒷정리
```

올리기 전에 확인한다.

- 워크스페이스 패키지를 더하거나 옮겼으면 **`apps/api/Dockerfile` 의 두 줄**을 함께 고쳤는가
- 계약(`packages/contracts`)을 고쳤으면 web·api 양쪽을 다 고쳤는가
- 규칙 파서를 고쳤으면 `apps/api` 테스트를 돌렸는가
- 정확도를 손댔으면 **고치기 전에 쓰던 문장 말고** 새 문장으로 다시 쟀는가

배포는 커밋을 쪼갤 이유가 되지 않는다. Vercel 은 작업 트리를 그대로 올리고,
Render 는 브랜치 끝을 본다 — 커밋 하나로 몇 번을 배포하든 상관없다.

## 화면을 손대기 전에

`docs/design-reference/` 에 설계 원본이 화면별로 쪼개져 있다.
기억이나 추측이 아니라 파일을 열어 대조한다.

만들기 전에 그 화면이 개정 설계에 아직 있는지부터 확인한다.
22종에서 26종으로 개정되면서 빠진 화면이 있다.

## dev 가 도는 중에 build 를 돌리지 않는다

개발 서버와 빌드가 같은 산출물 폴더를 쓴다. 켜둔 채로 빌드하면 서로 덮어써서
깨진다. 웹은 화면이 200 으로 뜨는데 CSS 와 청크가 404 가 되고(스타일 없는 날것
화면이 나오면 이걸 의심한다), API 는 dist 가 반쯤 지워져 기동조차 못 한다.

  apps/web   next dev  ↔  next build   .next
  apps/api   nest start --watch ↔ nest build   dist

타입만 보려면 `typecheck` 를 쓴다. 굳이 빌드해야 하면 개발 서버를 먼저 멈추고,
깨졌으면 산출물 폴더를 지우고 다시 띄운다.

## 고쳤으면 띄워서 본다

타입체크와 빌드가 통과해도 화면은 틀릴 수 있다. 실제로 실행해 눈으로 본다.
이 저장소에서 그렇게 잡은 버그가 여럿이다 — 헤드라인 숫자가 카드 수와
달랐던 것, 쉬어가기가 제자리에 남던 것, 설계에 없는 줄이 남아 있던 것.
